Place the university seal or other logo image here so the app can show it on the landing page.

Filename: `uop_seal.png`

Recommended steps:
1. Save the attached image from the chat as `uop_seal.png`.
2. Move or copy it into this folder: `assets/uop_seal.png`.
3. Restart Streamlit: `streamlit run app.py`.

Notes:
- The app will automatically show the image when it exists.
- The sidebar includes color pickers to change the app's primary/background/text colors interactively.
